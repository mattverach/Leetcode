/**
 * @param {number} celsius
 * @return {number[]}
 */
var convertTemperature = function(celsius) {

    const output = new Array();

    output.push(celsius + 273.15, fahrenheit = celsius * 1.80 + 32.00);

    return output;

    

    
};